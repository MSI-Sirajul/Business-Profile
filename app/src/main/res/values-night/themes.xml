<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark Mode Theme -->
    <style name="Theme.BusinessProfile" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Dark Mode Colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/background</item> <!-- Status bar blends with bg -->
        <item name="colorAccent">@color/colorSecondary</item>
        
        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/background</item>
        
        <!-- Text Colors for automatic adaptation -->
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <style name="Theme.BusinessProfile.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>